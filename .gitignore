# ── Secrets — NEVER commit ─────────────────────────────
.env
.env.*
!.env.example
secrets/
*.pem
*.key
*.p8

# ── Python / Backend ───────────────────────────────────
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# ── Database (local SQLite) ────────────────────────────
*.sqlite3
*.sqlite3-journal
*.db
BE/data/*.sqlite3
BE/data/*.db

# ── OS / Editor cruft ──────────────────────────────────
.DS_Store

# ── Temp / scratch files ───────────────────────────────
*.bak
FE/_test2.txt
FE/_writetest.txt

# ── Disabled nested git repo (preserved BE history) ──
.git.disabled/

# ── Claude Code 로컬 설정 ──
.claude/settings.local.json

# ── 생성된 랜드마크 이미지 캐시 (런타임 산출물) ──
BE/data/landmark_images/

# ── Playwright MCP 세션 산출물 ──
.playwright-mcp/

# ── Office 임시 잠금 파일 ──
~$*
